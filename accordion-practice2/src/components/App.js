import { Header } from "./Header"
import { Accordion } from "./Accordion"

export const App = () => {
    return (
        <div className="container">
            <Header />
            <Accordion />
        </div>
    )
}